from flask import Flask

app = Flask(__name__)

def create_app():
    app = Flask(__name__)
    app.config['MR_RIDDLE'] = "SUSYimpostor"

    return app

app = create_app()

if __name__ == '__main__':
    app.run(debug=True)
